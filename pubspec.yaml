name: custom_mapper_workspace
description: "Workspace for custom_mapper packages - annotations and build runner."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.8.0 <4.0.0"

# This is a workspace pubspec that orchestrates the two packages
workspace:
  - packages/custom_mapper_annotation
  - packages/custom_mapper

dependencies:

dev_dependencies:
