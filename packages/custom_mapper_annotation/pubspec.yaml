name: custom_mapper_annotation
description: "Annotations for custom_mapper code generation package."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:

dev_dependencies:
  test: ^1.24.0

topics:
  - annotation
  - codegen
  - mapper
