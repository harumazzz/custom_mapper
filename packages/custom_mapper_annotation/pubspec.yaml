name: custom_mapper_annotation
description: "Annotations for custom_mapper code generation package."
version: 0.0.5
resolution: workspace
homepage: https://github.com/harumazzz/custom_mapper
repository: https://github.com/harumazzz/custom_mapper
issue_tracker: https://github.com/harumazzz/custom_mapper/issues

environment:
  sdk: ">=3.8.0 <4.0.0"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:

dev_dependencies:
  test: ^1.24.0

topics:
  - annotation
  - codegen
  - mapper
